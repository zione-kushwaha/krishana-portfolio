[build]
  command = "flutter build web"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "3.38.3"

# Removed the plugin section because it does not exist
# [[plugins]]
#   package = "@netlify/plugin-flutter"
